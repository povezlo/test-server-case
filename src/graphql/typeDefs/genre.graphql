type Genre {
  id: String!
  name: String!
  slug: String!
  createdAt: String!
  updatedAt: String!
}

extend type Query {
  genres: [Genre!]!
  genre(id: String!): Genre
  genreBySlug(slug: String!): Genre
}
